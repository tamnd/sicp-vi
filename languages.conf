# Supported language editions.
# Add a new line here (e.g. zh, ja) to enable a new translation.
# The value must match a directory name under books/ and the
# corresponding books/<lang>/sicp.texi must exist.
en_US
vi
