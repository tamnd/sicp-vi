name: Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            latexmk \
            texlive-xetex \
            texlive-latex-extra \
            texlive-fonts-extra \
            fonts-linuxlibertine \
            inkscape \
            perl \
            ruby \
            ruby-nokogiri \
            unzip \
            zip

      - name: Install Inconsolata LGC font
        run: |
          sudo mkdir -p /usr/local/share/fonts/inconsolata-lgc
          curl -fsSL "https://github.com/MihaiPopescu/Inconsolata-LGC/releases/download/1.3/Inconsolata-LGC.zip" \
            -o /tmp/inconsolata-lgc.zip
          sudo unzip -q /tmp/inconsolata-lgc.zip -d /usr/local/share/fonts/inconsolata-lgc/
          sudo fc-cache -fv

      - name: Build PDF
        run: make -C src/pdf

      - name: Build epub
        run: |
          touch src/html/*.xhtml
          make -C src epub

      - name: Package HTML
        run: |
          cd src && zip -r ../sicp-html.zip html/

      - name: Attach assets to release
        run: |
          gh release upload "${{ github.ref_name }}" \
            src/pdf/sicp.pdf \
            sicp.epub \
            sicp-html.zip
        env:
          GH_TOKEN: ${{ github.token }}
